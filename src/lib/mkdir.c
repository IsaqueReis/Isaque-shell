#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "mkdir.h"


int clear_path_str(char *str)
{
    char *res = NULL;
    
}

int nimps_mkdir(const char *path, mode_t mode)
{
    int ret = 0;                        
    char *tmp_dir;
    size_t len = strlen(path);

    if(len && path[len - 1] == '/')
    {
        path = path[i]
        tmp_dir = 
    }
}